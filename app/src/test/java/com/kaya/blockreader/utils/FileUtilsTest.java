package com.kaya.blockreader.utils;

import android.content.Context;
import android.test.mock.MockContext;

import com.kaya.blockreader.blockApplication;

import org.junit.Test;

import java.io.IOException;

import static org.junit.Assert.*;

public class FileUtilsTest {

    @Test
    public void readAssets() throws IOException {
/*        Context context = new MockContext();
        ApplicationProvider.getApplicationContext()
        blockApplication.getInstance().getAssets().open("胡雪岩全传.txt");
      //  FileUtils.readAssets(context,"胡雪岩全传.txt");*/
    }
}